import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        gap: 8
    },
    completedText: {
        fontWeight: 'bold',
        fontSize: 14,
        color: '#8284FA',
    }
})