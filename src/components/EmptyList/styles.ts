import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    container: {
        marginTop: 48,
        alignItems: 'center',
        justifyContent: 'center',
    },
    image: {
        width: 56,
        height: 56,
    },
    text: {
        fontSize: 14,
        textAlign: 'center',
        color: '#808080'
    }
})