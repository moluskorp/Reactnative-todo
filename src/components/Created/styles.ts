import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        gap: 8
    },
    createdText: {
        fontWeight: 'bold',
        fontSize: 14,
        color: '#4ea8de',
    },
})